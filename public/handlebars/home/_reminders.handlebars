<div id="reminders">

  <header id="reminderheader" class="homeheader">
    <h2 class="paneltitle">TODAY'S REMINDERS</h2>
    <div id="homeaddreminder" {{action "addReminder"}} >+ New Reminder</div>
  </header>

  <div class="plusminuswidget daywidget">
    <div class="plusminusprev" {{action "previousDay"}}>
      <
    </div>
    <div class="plusminuscur">
      {{dayName activeDay}}
      <br>
      {{monthDayYear activeDay}}
    </div>
    <div class="plusminusnext" {{action "nextDay"}}>
      >
    </div>
  </div>

  <ul id="reminderlist">
  {{#each reminder in controller.sortedFilteredReminders}}
    <li class="reminder">

      <div class="reminderdetails">
        <div class="remindernamebox">
          <span class="remindertag">TAG</span>
          <span class="reminderpersonname">{{reminder.person.firstName}}</span>
        </div>
        {{input type="checkbox" classNames="remindercheck"}}
        <div class="remindertime">{{timeToday reminder.targetDateTime}}</div>
      </div>

      <div class="remindernote">
        {{reminder.notes}}
      </div>

    </li>
  {{/each}}
  </ul>

</div>
